using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SurfaceFrictionDirection : MonoBehaviour
{
    //
    [Range(0,100)] public float TopFriction; 
    [Range(0,100)] public float BottomFriction; 
    [Range(0,100)] public float LeftFriction; 
    [Range(0,100)] public float RightFriction; 
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
